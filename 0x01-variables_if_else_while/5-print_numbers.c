#include <stdio.h>

/**
 * main - Function tha print the number from 0 to 9
 *
 * Return: Always (0) sucess
 */
int main(void)
{
	int num;

	for (num = 48; num <= 57; num++)
	{
		putchar(num);
	}
	putchar(10);
	return (0);
}
