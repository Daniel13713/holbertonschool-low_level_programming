#include "main.h"

/**
 * main - prints the name of file compile
 *
 * @argc: number of atributes on command line
 * @argv: array of string of the command line
 * Return: always (0) succes
 */

int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}

